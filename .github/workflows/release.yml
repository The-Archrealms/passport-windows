name: release

on:
  workflow_dispatch:
    inputs:
      tag:
        description: Release tag to publish, for example v0.1.0
        required: true
        type: string
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  package:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 8.0.x

      - name: Resolve release tag
        id: release_tag
        shell: pwsh
        run: |
          if ("${{ github.event_name }}" -eq "workflow_dispatch") {
            $tag = "${{ inputs.tag }}"
          }
          else {
            $tag = "${{ github.ref_name }}"
          }

          if (-not $tag) {
            throw "Release tag could not be resolved."
          }

          "tag=$tag" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append

      - name: Package win-x64 release
        shell: pwsh
        run: .\tools\release\Publish-PassportWindows.ps1 -DotnetPath (Get-Command dotnet).Source -RuntimeIdentifier win-x64 -Version "${{ steps.release_tag.outputs.tag }}"

      - name: Package win-x64 MSIX
        shell: pwsh
        env:
          PASSPORT_WINDOWS_MSIX_PFX_BASE64: ${{ secrets.PASSPORT_WINDOWS_MSIX_PFX_BASE64 }}
          PASSPORT_WINDOWS_MSIX_PFX_PASSWORD: ${{ secrets.PASSPORT_WINDOWS_MSIX_PFX_PASSWORD }}
        run: .\tools\release\Publish-PassportWindowsMsix.ps1 -Version "${{ steps.release_tag.outputs.tag }}" -Platform x64 -SkipSignatureVerification

      - name: Upload release bundle
        uses: actions/upload-artifact@v6
        with:
          name: passport-windows-win-x64
          path: artifacts/release/passport-windows-win-x64

      - name: Upload MSIX bundle
        uses: actions/upload-artifact@v6
        with:
          name: passport-windows-msix-x64
          path: artifacts/release/passport-windows-msix/x64

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.release_tag.outputs.tag }}
          target_commitish: ${{ github.sha }}
          generate_release_notes: true
          overwrite_files: true
          files: |
            artifacts/release/passport-windows-win-x64/passport-windows-win-x64.zip
            artifacts/release/passport-windows-win-x64/release-manifest.json
            artifacts/release/passport-windows-msix/x64/passport-windows-x64.msix
            artifacts/release/passport-windows-msix/x64/passport-windows-signing.cer
            artifacts/release/passport-windows-msix/x64/msix-package-manifest.json
